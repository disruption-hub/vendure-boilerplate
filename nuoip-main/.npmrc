legacy-peer-deps=true
# Suppress npm production warning - use --omit=dev instead of deprecated --production flag
# Note: This may not fully suppress the warning when NODE_ENV=production is set
# The warning is harmless and can be ignored
loglevel=warn
